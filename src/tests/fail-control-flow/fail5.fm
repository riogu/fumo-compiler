// Else-if chain failures
fn test_else_if_middle_missing(x: i32) -> i32 {
    if x > 10 {
        return 1;
    }
    else if x > 0 {
        let y = x; // No return!
    }
    else {
        return 0;
    }
}
