fn main() -> i32 {
    let ptr: i32* = nullptr;
    let value = 42;
    let valid_ptr: i32* = &value;
    
    if (ptr == nullptr) {
        if (valid_ptr != nullptr) {
            return 1;
        }
    }
    return 0;
}
