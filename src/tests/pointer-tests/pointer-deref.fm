fn main() -> i32 {
    let value = 42;
    let ptr: i32* = &value;
    
    if (ptr) {
        if (*ptr > 40) {
            *ptr = 100;
            if (*ptr == 100) {
                return 1;
            }
        }
    }
    return 0;
}
