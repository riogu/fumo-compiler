// Empty string literal
fn printf(format: i8*, ...) -> i32;
fn strlen(s: i8*) -> i64;  // size_t is typically 64-bit
fn main() -> i32 {
    let empty: i8* = "";
    printf("Empty string length: %d\n", strlen(empty));
    return 0;
}
