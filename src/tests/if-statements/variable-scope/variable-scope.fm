fn main() -> i32 {
    let outer = 10;
    
    if (outer > 5) {
        let inner = 20;
        if (inner > outer) {
            let deeply_nested = 30;
            return deeply_nested;
        }
        return inner;
    }
    return outer;
}
