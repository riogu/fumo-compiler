fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn multiply(a: i32, b: i32) -> i32 {
    return a * b;
}

fn main() -> i32 {
    let x = 5;
    let y = 3;
    
    if (add(x, y) > multiply(x, 2)) {
        return 1;
    } else {
        return 0;
    }
}
