fn scanf (format: i8* , ... ) -> i32;
fn printf (format: i8* , ... ) -> i32;

fn fib(n: i64) -> i64 {
    if n < 2 {
        return n; 
    } 
    return fib(n - 1) + fib(n - 2);
}

fn main() -> void {
    let var: i64;
    scanf("%ld", &var);
    printf("%ld\n", fib(var));
}
