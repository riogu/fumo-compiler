fn printf(format: i8*, ...) -> i32;

fn main() -> i32 {
    let any_ptr: any*;
    
    // This should FAIL - cannot do arithmetic on any*
    any_ptr = any_ptr + 1;  // ERROR: can't do arithmetic on any*
    
    return 0;
}
