// Empty string literal
fn printf(format: char*, ...) -> i32;
fn strlen(s: char*) -> i64;  // size_t is typically 64-bit
fn main() -> i32 {
    let empty: char* = "";
    printf("Empty string length: %d\n", strlen(empty));
    return 0;
}
