// // Sequential tests
// fn test_sequential_return_after(x: i32) -> i32 {
//     if x > 0 {
//         let y = x * 2;
//     }
//     
//     let z = 42;
//     return z;
// }
// Nested if tests
fn test_nested_if_all_return(x: i32, y: i32) -> i32 {
    if x > 0 {
        if y > 0 {
            return 1;
        } else {
            return 2;
        }
    } else {
        return 0;
    }
}

